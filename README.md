# HDB Carpark Info App

A simple Flask app that displays HDB carpark information from data.gov.sg.

## Setup

1. Clone this repo:
   ```bash
   git clone https://github.com/your-username/carpark-app.git
   cd carpark-app

2. Install dependencies:
   pip install -r requirements.txt

3. Run locally
   python app.py
